# Rollback

Os builders anteriores permanecem no repositório. Para rollback do editor, basta apontar `pages/CompanyPageBuilder.tsx` novamente para `CompanyPageBuilderV2`.

O renderer central continua aceitando os mesmos campos de configuração persistidos, portanto o rollback não exige alteração de banco.
