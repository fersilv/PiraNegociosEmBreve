# Brand asset generation

The PiraNegócios visual assets are generated by `scripts/generate_brand_assets.py`.

- All PNG exports have transparent backgrounds.
- Horizontal logo variants, monochrome variants, isolated symbols, favicons and notification badges are generated into `assets/logos/`.
- GitHub Actions keeps the generated files versioned in the repository.
